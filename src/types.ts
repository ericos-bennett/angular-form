export type Textbox = {
  id: string,
  title: string,
  placeholder: string
}

export type Dropdown = {
  id: string,
  title: string,
  options: string[]
}

export type Checkboxes = {
  id: string,
  title: string,
  options: string[]
}

export type Radio = {
  id: string,
  title: string,
  options: string[]
}
